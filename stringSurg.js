let sentence = "I like rice and stew!"
console.log(sentence.toUpperCase());

// some string methods //
let myString = "This was really great, let's do it again..."
realStr = myString.search("really");
console.log(myString.substr(realStr, realStr.length));
console.log(myString.replace("really", "very"));